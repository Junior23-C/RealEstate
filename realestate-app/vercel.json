{
  "buildCommand": "cp prisma/schema.prod.prisma prisma/schema.prisma && prisma generate && prisma migrate deploy && next build",
  "env": {
    "PRISMA_SCHEMA_PATH": "./prisma/schema.prod.prisma"
  }
}