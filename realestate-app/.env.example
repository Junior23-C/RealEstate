# Database
# For local development (SQLite)
DATABASE_URL="file:./dev.db"

# For production (PostgreSQL - get from services like Neon, Supabase, or Vercel Postgres)
# DATABASE_URL="postgresql://username:password@hostname:port/database?sslmode=require"
# ⚠️ NEVER commit your actual database credentials to git!

# NextAuth.js Configuration
NEXTAUTH_SECRET="generate-strong-secret-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# For Vercel deployment, set NEXTAUTH_URL to your production domain:
# NEXTAUTH_URL="https://your-domain.vercel.app"

# Admin Credentials (CHANGE THESE IN PRODUCTION!)
ADMIN_EMAIL="admin@yourdomain.com"
ADMIN_PASSWORD="change-this-to-a-very-strong-password"

# Security Note: Generate secure values for production:
# NEXTAUTH_SECRET: Run 'openssl rand -base64 32' to generate
# ADMIN_PASSWORD: Use a strong, unique password (minimum 12 characters)
# ADMIN_EMAIL: Use your actual admin email address